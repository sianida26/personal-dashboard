---
description: 
globs: 
alwaysApply: true
---
- Use the null‑coalescing operator (??) instead of ||.
- Avoid using the `any` type.
- Ensure the UI and UX are optimal.
- Review each change for security vulnerabilities.
- environment variables for backend should be retrieved from [appEnv.ts](mdc:apps/backend/src/appEnv.ts) 
- do not run migrations automatically
- do not run commands for starting server