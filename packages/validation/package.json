{
	"name": "@repo/validation",
	"type": "module",
	"private": true,
	"scripts": {
		"lint": "biome lint --write --diagnostic-level warn",
		"format": "biome format --write --diagnostic-level warn",
		"check": "tsc --noEmit && bun lint && bun format"
	},
	"exports": {
		".": "./src/index.ts"
	},
	"dependencies": {
		"@repo/data": "workspace:*",
		"zod": "^4.1.12"
	},
	"devDependencies": {
		"@repo/typescript-config": "workspace:*"
	}
}
